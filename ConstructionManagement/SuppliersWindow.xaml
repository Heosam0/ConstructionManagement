<Window x:Class="ConstructionManagement.SuppliersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
       Title="Управление поставщиками" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid x:Name="SuppliersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" />
                <DataGridTextColumn Header="Организация" Binding="{Binding OrganizationName}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*" />
                <DataGridTextColumn Header="Оборудование" Binding="{Binding Equipment.Count}" Width="Auto" />
                <DataGridTextColumn Header="Материалы" Binding="{Binding Materials.Count}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Добавить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="0,0,10,10" Click="AddSupplier_Click" />
        <Button Content="Редактировать" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="142" Margin="110,0,0,10" Click="EditSupplier_Click" />
        <Button Content="Удалить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="272,0,0,10" Click="DeleteSupplier_Click" />
    </Grid>
</Window>
