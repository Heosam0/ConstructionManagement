<Window x:Class="ConstructionManagement.AddEditPaymentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
          Title="{Binding WindowTitle}" Height="350" Width="450"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Сумма -->
        <Label Content="Сумма:" Grid.Row="0" />
        <TextBox x:Name="AmountTextBox" Grid.Row="1" Text="{Binding Payment.Amount, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Тип -->
        <Label Content="Тип:" Grid.Row="2" />
        <ComboBox x:Name="TypeComboBox" Grid.Row="3"
                SelectedItem="{Binding Payment.Type, UpdateSourceTrigger=PropertyChanged}">
        </ComboBox>

        <!-- Дата -->
        <Label Content="Дата:" Grid.Row="4" />
        <DatePicker x:Name="DatePicker" Grid.Row="5" SelectedDate="{Binding Payment.Date, UpdateSourceTrigger=PropertyChanged}" Height="28" VerticalAlignment="Top" />

        <!-- Контракт -->
        <Label Content="Контракт:" Grid.Row="5" Margin="0,28,0,0" Height="30" VerticalAlignment="Top" />
        <ComboBox x:Name="ContractComboBox" Grid.Row="5"
                  DisplayMemberPath="Name"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Payment.ContractId, UpdateSourceTrigger=PropertyChanged}" Margin="0,53,0,0" Height="30" VerticalAlignment="Top" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
