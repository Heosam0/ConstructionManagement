<Window x:Class="ConstructionManagement.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
      Title="Отчеты" Height="450" Width="800"
    ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Кнопка экспорта -->
        <Button Content="Экспорт в Excel" HorizontalAlignment="Right" Margin="0,0,10,10" Width="120" Click="ExportToExcel_Click" />

        <!-- DataGrid для отображения данных -->
        <DataGrid x:Name="ReportDataGrid" AutoGenerateColumns="False" Margin="10" Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название проекта" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Заказчик" Binding="{Binding Customers.OrganizationName}" Width="*" />
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}" Width="Auto" />
                <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat={}{0:dd.MM.yyyy}}" Width="Auto" />
                <DataGridTextColumn Header="Количество задач" Binding="{Binding Tasks.Count}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
