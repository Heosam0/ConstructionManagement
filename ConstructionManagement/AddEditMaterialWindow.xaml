<Window x:Class="ConstructionManagement.AddEditMaterialWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
      Title="{Binding WindowTitle}" Height="439" Width="536"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Название -->
        <Label Content="Название:" Grid.Row="0" />
        <TextBox x:Name="NameTextBox" Grid.Row="1" Text="{Binding Material.Name, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Количество -->
        <Label Content="Количество:" Grid.Row="2" />
        <TextBox x:Name="QuantityTextBox" Grid.Row="3" Text="{Binding Material.Quantity, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Цена -->
        <Label Content="Цена:" Grid.Row="4" />
        <TextBox x:Name="PriceTextBox" Grid.Row="5" Text="{Binding Material.Price, UpdateSourceTrigger=PropertyChanged}" Height="27" VerticalAlignment="Top" />

        <!-- Поставщик -->
        <Label Content="Поставщик:" Grid.Row="5" Margin="0,28,0,0" Height="30" VerticalAlignment="Top" />
        <ComboBox x:Name="SupplierComboBox" Grid.Row="5"
                  DisplayMemberPath="OrganizationName"
                  SelectedValuePath="Id"
                  SelectedValue="{Binding Material.SupplierId, UpdateSourceTrigger=PropertyChanged}" Margin="0,55,0,0" Height="30" VerticalAlignment="Top" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="116" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
