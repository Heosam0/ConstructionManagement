<Window x:Class="ConstructionManagement.AddEditSupplierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
         Title="{Binding WindowTitle}" Height="350" Width="450"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Организация -->
        <Label Content="Организация:" Grid.Row="0" />
        <TextBox x:Name="OrganizationNameTextBox" Grid.Row="1" Text="{Binding Supplier.OrganizationName, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Email -->
        <Label Content="Email:" Grid.Row="2" />
        <TextBox x:Name="EmailTextBox" Grid.Row="3" Text="{Binding Supplier.Email, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Телефон -->
        <Label Content="Телефон:" Grid.Row="4" Height="196" VerticalAlignment="Top" />
        <TextBox x:Name="PhoneTextBox" Grid.Row="4" Text="{Binding Supplier.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,28,0,0" Height="21" VerticalAlignment="Top" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="80" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>
