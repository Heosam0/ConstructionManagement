<Window x:Class="ConstructionManagement.AddEditCustomerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
         Title="{Binding WindowTitle}" Height="496" Width="500"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Фамилия -->
        <Label Content="Фамилия:" Grid.Row="0" />
        <TextBox x:Name="LastNameTextBox" Grid.Row="1" Text="{Binding Customer.LastName, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Имя -->
        <Label Content="Имя:" Grid.Row="2" />
        <TextBox x:Name="FirstNameTextBox" Grid.Row="3" Text="{Binding Customer.FirstName, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Отчество -->
        <Label Content="Отчество:" Grid.Row="4" />
        <TextBox x:Name="PatronymicTextBox" Grid.Row="5" Text="{Binding Customer.Patronymic, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Организация -->
        <Label Content="Организация:" Grid.Row="6" />
        <TextBox x:Name="OrganizationNameTextBox" Grid.Row="7" Text="{Binding Customer.OrganizationName, UpdateSourceTrigger=PropertyChanged}" Height="24" VerticalAlignment="Top" />

        <!-- Email -->
        <Label Content="Email:" Grid.Row="7" Margin="0,89,0,0" Height="30" VerticalAlignment="Top" />
        <TextBox x:Name="EmailTextBox" Grid.Row="7" Text="{Binding Customer.Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,119,0,0" Height="30" VerticalAlignment="Top" />

        <!-- Телефон -->
        <Label Content="Телефон:" Grid.Row="7" Margin="0,29,0,0" Height="30" VerticalAlignment="Top" />
        <TextBox x:Name="PhoneTextBox" Grid.Row="7" Text="{Binding Customer.Phone, UpdateSourceTrigger=PropertyChanged}" Margin="0,58,0,0" Height="30" VerticalAlignment="Top" />

        <!-- Кнопки -->
        <StackPanel Grid.Row="12" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Сохранить" Width="127" Margin="0,0,10,0" Click="SaveButton_Click" />
            <Button Content="Отмена" Width="80" Click="CancelButton_Click" />
        </StackPanel>
    </Grid>
</Window>