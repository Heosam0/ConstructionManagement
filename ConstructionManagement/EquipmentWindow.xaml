<Window x:Class="ConstructionManagement.EquipmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
          Title="Управление оборудованием" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid x:Name="EquipmentDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" />
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Дата покупки" Binding="{Binding DateOfPurchase, StringFormat={}{0:dd.MM.yyyy}}" Width="Auto" />
                <DataGridTextColumn Header="Стоимость" Binding="{Binding Cost}" Width="Auto" />
                <DataGridTextColumn Header="Поставщик" Binding="{Binding Supplier.OrganizationName}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Добавить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="0,0,10,10" Click="AddEquipment_Click" />
        <Button Content="Редактировать" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="140" Margin="110,0,0,10" Click="EditEquipment_Click" />
        <Button Content="Удалить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="255,0,0,10" Click="DeleteEquipment_Click" />
    </Grid>
</Window>