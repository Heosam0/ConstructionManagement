<Window x:Class="ConstructionManagement.CustomersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ConstructionManagement"
        mc:Ignorable="d"
          Title="Управление заказчиками" Height="450" Width="800">
    <Grid Margin="10">
        <DataGrid x:Name="CustomersDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,0,50">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="Auto" />
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="*" />
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="*" />
                <DataGridTextColumn Header="Отчество" Binding="{Binding Patronymic}" Width="*" />
                <DataGridTextColumn Header="Организация" Binding="{Binding OrganizationName}" Width="*" />
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*" />
                <DataGridTextColumn Header="Телефон" Binding="{Binding Phone}" Width="*" />
                <DataGridTextColumn Header="Контракты" Binding="{Binding Contracts.Count}" Width="Auto"/>
                <DataGridTextColumn Header="Проекты" Binding="{Binding Projects.Count}" Width="Auto" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="Добавить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="0,0,10,10" Click="AddCustomer_Click" />
        <Button Content="Редактировать" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="136" Margin="110,0,0,10" Click="EditCustomer_Click" />
        <Button Content="Удалить" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="100" Margin="265,0,0,10" Click="DeleteCustomer_Click" />
    </Grid>
</Window>